spring:
  datasource:
    url: jdbc:h2:mem:schoolapp
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true

server:
  port: 8080

app:
  admin:
    email: admin@demo.com
    password: admin123
